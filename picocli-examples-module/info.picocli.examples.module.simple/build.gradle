version "$projectVersion"
ext.moduleName = 'info.picocli.examples.module.simple'
ext.mainClassName = 'picocli.jpmscli.ModularCli'

application {
    mainModule = "$moduleName"
    mainClass = 'picocli.jpmscli.ModularCli'
}

dependencies {
    api "info.picocli:picocli:${project.version}"
}
